# plugin: kubevirt.core.kubevirt
# connections:
#   # validate_certs: false
#   - namespaces:
#      - vmimported
---
plugin: redhat.openshift_virtualization.kubevirt
strict: true
connections:
  - namespaces:
      - vmimported
